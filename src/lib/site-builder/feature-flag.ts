// Site Builder is fully built but deliberately locked from real usage for
// now. Every dashboard entry point and every mutating API route gates on
// this single flag — flip it back to true to re-enable the whole feature.
export const SITE_BUILDER_ENABLED = false
